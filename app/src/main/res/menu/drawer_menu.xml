<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_home"
            android:title="Home" />
        <item
            android:id="@+id/nav_about"
            android:icon="@drawable/ic_info"
            android:title="About" />
    </group>

    <item android:title="Admin Menu" android:id="@+id/menu_admin_section">
        <menu>
            <item
                android:id="@+id/nav_manage_lapangan"
                android:icon="@drawable/ic_edit"
                android:title="Manage Lapangan" />
            <item
                android:id="@+id/nav_manage_booking"
                android:icon="@drawable/ic_agenda"
                android:title="Manage Booking" />
        </menu>
    </item>

    <item android:title="Account">
        <menu>
            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/ic_logout"
                android:title="Logout" />
        </menu>
    </item>
</menu>